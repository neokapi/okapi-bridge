{
  "$schema": "../overlay.schema.json",
  "id": "concordance-character-count",
  "kind": "step",
  "name": "Concordance Character Count Step",
  "overview": "Counts the characters in text units whose top alternate translation is a `CONCORDANCE` match — a translation-memory concordance or phrase hit, typically covering only a word or term rather than a full segment. It is the character-counting counterpart of the Concordance Word Count Step, differing only in that it tallies characters rather than words.",
  "limitations": [
    "The Okapi source notes that, as of its writing, no leveraging step yet annotates segments with the `CONCORDANCE` match type, so in practice this counter typically reports zero unless an upstream component supplies such matches."
  ],
  "processingNotes": [
    "Operates per segment: it inspects the first (top-ranked) `AltTranslation` on each target segment's `AltTranslationsAnnotation` and counts a segment only when that match type is exactly `CONCORDANCE`.",
    "Counting is performed on the **source** text, not the target — the character total reflects the source volume leveraged at this match level.",
    "Uses the GMX-V character counter; the only behavioural difference from the word-count variant is words versus characters.",
    "Counts only translatable text units; empty text units and non-translatable ones are skipped.",
    "Results are stored as annotations on filter events and aggregated to the configured levels (text unit, group, document, sub-document, batch item, batch); the step produces no output document of its own.",
    "Belongs to the `OKAPI_CHARACTER_COUNTS` category group. The `CategoryResolver` keeps only the highest-ranked matching category per segment — `CONCORDANCE` is the lowest-ranked of the leveraging match categories, below `MT` and all TM-derived categories."
  ],
  "x-provenance": {
    "generator": "claude-opus-4-7",
    "grounding": [
      "okapi-source:okapi/steps/wordcount/src/main/java/net/sf/okapi/steps/wordcount/categorized/okapi/ConcordanceCharacterCountStep.java",
      "okapi-source:okapi/steps/wordcount/src/main/java/net/sf/okapi/steps/wordcount/categorized/okapi/ConcordanceWordCountStep.java",
      "okapi-source:okapi/steps/wordcount/src/main/java/net/sf/okapi/steps/wordcount/common/AltAnnotationBasedCountStep.java",
      "okapi-source:okapi/steps/wordcount/src/main/java/net/sf/okapi/steps/wordcount/common/BaseCountStep.java",
      "okapi-source:okapi/steps/wordcount/src/main/java/net/sf/okapi/steps/wordcount/categorized/CategoryResolver.java",
      "okapi-source:okapi/core/src/main/java/net/sf/okapi/common/query/MatchType.java",
      "schema"
    ],
    "authoredAt": "2026-05-23",
    "reviewed": false
  }
}
