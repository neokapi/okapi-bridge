{
  "$schema": "../overlay.schema.json",
  "id": "okf_xliff2",
  "kind": "filter",
  "name": "XLIFF-2 Filter",
  "overview": "The XLIFF-2 Filter implements the `IFilter` interface for **XLIFF 2.x** (XML Localisation Interchange File Format) documents. XLIFF v2 is an OASIS Standard for transporting translatable text and localization-related information across translation and localization tool chains. The filter provides basic support for XLIFF 2.x core including extended attributes/namespaces, segments and ignorables, inline codes, notes, groups, and the XLIFF 2.x Metadata Module.",
  "limitations": [
    "Skeleton not supported.",
    "Comments are lost in the merged document.",
    "Original XML formatting is lost in the merged document.",
    "Attributes can be reordered.",
    "Attributes may be removed or added compared to the original depending on default values and logic in the XLIFF 2 Toolkit."
  ],
  "processingNotes": [
    "Supports XLIFF 2.x core features: extended attributes and namespaces, segments and ignorables, inline codes, notes, groups, and the XLIFF 2.x Metadata Module.",
    "Merged documents will not preserve original XML formatting, comments, or attribute ordering."
  ],
  "examples": [
    {
      "title": "Strict validation on ingest",
      "description": "Parse with maximum format verification (the default) and reject files whose targets are invalid, so malformed XLIFF 2 is caught before it enters the pipeline rather than being silently accepted.",
      "config": "```yaml\noutput:\n  maxValidation: true\nstates:\n  discardInvalidTargets: false\n```"
    },
    {
      "title": "Tolerant ingest of imperfect XLIFF 2",
      "description": "Read files produced by tools that emit duplicate tag IDs and mismatched source/target tag types, discarding individual invalid targets instead of failing the whole file. Useful for salvaging content from non-conformant exports.",
      "config": "```yaml\nextraction:\n  forceUniqueIds: true\n  ignoreTagTypeMatch: true\nstates:\n  discardInvalidTargets: true\n```"
    },
    {
      "title": "Drop original data on output",
      "description": "Write merged output without the `<originalData>` store that backs inline codes. Produces leaner files when downstream tools do not need the verbatim original markup behind each code.",
      "config": "```yaml\noutput:\n  writeOriginalData: false\n```"
    }
  ],
  "parameters": {
    "maxValidation": {
      "help": "Ensures the XLIFF-2 parser performs the **maximum verification** of the format during parsing. Useful for catching malformed or non-compliant XLIFF 2.x files early in the pipeline."
    },
    "needsSegmentation": {
      "help": "Resegments any unit that is marked with `canSegment=\"yes\"` in the XLIFF source. Units that already have a `<target>` element **cannot** be segmented, even if `canSegment=\"yes\"` is set.",
      "notes": [
        "Units with an existing target cannot be segmented, regardless of this setting."
      ]
    },
    "extraction.forceUniqueIds": {
      "help": "Forces inline-tag IDs to be unique within each unit on both read and write. On read, it is passed to the XLIFF 2 Toolkit reader so duplicate code IDs are disambiguated; it is also passed to the inline code finder so codes it injects receive unique IDs; on write, the toolkit writer renumbers IDs to keep them unique. Enable this for documents whose authoring tool emitted repeated tag IDs within a unit, which would otherwise break tag matching.",
      "values": "Boolean. Default `false`."
    },
    "extraction.ignoreTagTypeMatch": {
      "help": "Relaxes the reader's validation that an inline tag in the `<target>` has the same type as its counterpart in the `<source>`. Passed to the XLIFF 2 Toolkit reader. When disabled (default), a source/target tag-type mismatch is reported as an error; enable this to tolerate files where targets use a differing tag type for the corresponding code.",
      "values": "Boolean. Default `false`."
    },
    "extraction.simplifyTags": {
      "help": "Declared but not consumed by the current filter implementation — the flag is defined on the parameters object and defaults to `false`, but it is not surfaced in the filter's editor and is not read by the reader, writer, or converter. It is retained for configuration compatibility and has no effect at runtime.",
      "values": "Boolean. Default `false`.",
      "notes": [
        "No-op in the current Okapi xliff2 filter: no reader/writer code path reads this flag."
      ]
    },
    "output.writeOriginalData": {
      "help": "Controls whether the original-data store (the `<originalData>` content that backs inline codes via `<data>` references) is written to the output when present. Passed to the XLIFF 2 Toolkit writer as `setWithOriginalData`. Enabled by default so round-tripped files retain the verbatim original markup behind each code; disable it only when the original data should be dropped from the output.",
      "values": "Boolean. Default `true`."
    },
    "states.discardInvalidTargets": {
      "help": "Determines how the reader reacts to a `<target>` that fails validation. Passed to the XLIFF 2 Toolkit reader. When enabled, an invalid target is discarded and parsing continues; when disabled (default), the invalid target causes the file to be rejected. Enable this to salvage usable content from files containing some malformed targets.",
      "values": "Boolean. Default `false`."
    }
  },
  "propertySuggestions": {
    "maxValidation": {
      "title": "Maximum Validation",
      "description": "Perform maximum validation of the XLIFF 2.x format when parsing."
    },
    "needsSegmentation": {
      "title": "Needs Segmentation",
      "description": "Resegment units marked with canSegment=\"yes\" that do not already have a target."
    }
  },
  "fullDoc": "# XLIFF-2 Filter\n\nThe XLIFF-2 Filter implements the `IFilter` interface for **XLIFF 2.x** (XML Localisation Interchange File Format) documents. XLIFF v2 is an OASIS Standard for transporting translatable text and localization-related information across translation and localization tool chains. The filter provides basic support for XLIFF 2.x core including extended attributes/namespaces, segments and ignorables, inline codes, notes, groups, and the XLIFF 2.x Metadata Module.\n\n## Parameters\n\n#### Maximum Validation\nEnsures the XLIFF-2 parser performs the **maximum verification** of the format during parsing. Useful for catching malformed or non-compliant XLIFF 2.x files early in the pipeline.\n\n#### Needs Segmentation\nResegments any unit that is marked with `canSegment=\"yes\"` in the XLIFF source.\n\n> **Note:** Units that already have a `<target>` element **cannot** be segmented, even if `canSegment=\"yes\"` is set.\n\n## Limitations\n\n- Skeleton not supported.\n- Comments are lost in the merged document.\n- Original XML formatting is lost in the merged document.\n- Attributes can be reordered.\n- Attributes may be removed or added compared to the original depending on default values and logic in the XLIFF 2 Toolkit.\n\n## Notes\n\n- The filter supports XLIFF 2.x core features: extended attributes and namespaces, segments and ignorables, inline codes, notes, groups, and the XLIFF 2.x Metadata Module.\n- Merged documents will not preserve original XML formatting, comments, or attribute ordering.\n- The XLIFF 2.0 specification is available at the OASIS website.",
  "wikiUrl": "https://okapiframework.org/wiki/index.php/XLIFF-2_Filter",
  "x-provenance": {
    "generator": "claude-opus-4-7",
    "grounding": [
      "wiki:https://okapiframework.org/wiki/index.php/XLIFF-2_Filter",
      "okapi-source:okapi/filters/xliff2/src/main/java/net/sf/okapi/filters/xliff2/Parameters.java",
      "okapi-source:okapi/filters/xliff2/src/main/java/net/sf/okapi/filters/xliff2/XLIFF2Filter.java",
      "okapi-source:okapi/filters/xliff2/src/main/java/net/sf/okapi/filters/xliff2/XLIFF2FilterWriter.java",
      "schema"
    ],
    "authoredAt": "2026-05-23",
    "reviewed": false
  }
}
