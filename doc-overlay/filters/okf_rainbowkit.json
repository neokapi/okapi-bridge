{
  "$schema": "../overlay.schema.json",
  "id": "okf_rainbowkit",
  "kind": "filter",
  "name": "Rainbow Translation Kit Filter",
  "overview": "The Rainbow Translation Kit Filter implements the `IFilter` interface for reading packages generated by the Rainbow Translation Kit Creation Step. It processes translation kit packages by reading their `manifest.rkm` file. This is a **read-only** filter — it cannot re-write translation kits, only read them for input processing.",
  "limitations": [
    "This is a **read-only** filter. It does not generate output corresponding to the input — it must be used for input only.",
    "Cannot re-write or regenerate translation kit packages."
  ],
  "processingNotes": [
    "Rainbow translation kits are directory-based packages with a specific layout. The `manifest.rkm` file in the directory root serves as the entry point for processing the entire package.",
    "Different types of translation kit packages exist, each with its own directory structure and content."
  ],
  "examples": [
    {
      "title": "Processing a translation kit",
      "description": "To process a Rainbow translation kit, point the filter at the `manifest.rkm` file inside the kit directory — not at individual files within the package.",
      "input": "```\nmyproject-tkit/\n├── manifest.rkm      ← use this as input\n├── original/\n│   └── ...\n└── work/\n    └── ...\n```"
    }
  ],
  "parameters": {
    "general.openManifest": {
      "help": "An interactive option that, when enabled (the default), opens the package's `manifest.rkm` in an editor dialog before processing so a user can review or trim the file list — for example to merge back only a subset of the kit. It drives a Swing/SWT dialog and has no effect when the filter runs without a UI; in a headless or automated context leave it at the default and edit the manifest beforehand if selective processing is needed.",
      "values": "`true` / `false` (default `true`)",
      "notes": [
        "Requires a graphical environment; the dialog is skipped where no UI is available.",
        "The manifest file (`manifest.rkm`) is the entry point for the entire translation kit package — it must be the input file used to process the package."
      ],
      "seeAlso": "Rainbow_Translation_Kit_Manifest"
    }
  },
  "fullDoc": "# Rainbow Translation Kit Filter\n\nThe Rainbow Translation Kit Filter implements the `IFilter` interface for reading packages generated by the Rainbow Translation Kit Creation Step. It processes translation kit packages by reading their `manifest.rkm` file. This is a **read-only** filter — it cannot re-write translation kits, only read them for input processing.\n\n## Parameters\n\n### Open Manifest Before Processing\n\nOpens the `manifest.rkm` file before processing begins, allowing you to inspect or modify it. This is useful for selectively merging back only a subset of files listed in the manifest rather than processing the entire package.\n\nSee the [Rainbow Translation Kit Manifest](Rainbow_Translation_Kit_Manifest) page for details on the manifest format and how to edit it.\n\n> **Note:** The manifest file (`manifest.rkm`) is the entry point for the entire translation kit package — it must be used as the input file to process the package.\n\n## Limitations\n\n- This is a **read-only** filter. It does not generate output corresponding to the input — it must be used for input only.\n- Cannot re-write or regenerate translation kit packages.\n\n## Notes\n\n- Rainbow translation kits are directory-based packages with a specific layout. The `manifest.rkm` file in the directory root serves as the entry point for processing the entire package.\n- Different types of translation kit packages exist, each with its own directory structure and content.\n\n## Examples\n\n### Processing a translation kit\n\nTo process a Rainbow translation kit, point the filter at the `manifest.rkm` file inside the kit directory — not at individual files within the package.\n\n```\nmyproject-tkit/\n├── manifest.rkm      ← use this as input\n├── original/\n│   └── ...\n└── work/\n    └── ...\n```",
  "wikiUrl": "https://okapiframework.org/wiki/index.php/Rainbow_Translation_Kit_Filter",
  "x-provenance": {
    "generator": "claude-opus-4-7",
    "grounding": [
      "okapi-source:okapi/filters/rainbowkit/src/main/java/net/sf/okapi/filters/rainbowkit/Parameters.java",
      "okapi-source:okapi/filters/rainbowkit/src/main/java/net/sf/okapi/filters/rainbowkit/RainbowKitFilter.java",
      "wiki:https://okapiframework.org/wiki/index.php/Rainbow_Translation_Kit_Filter",
      "schema"
    ],
    "authoredAt": "2026-05-23",
    "reviewed": false
  }
}
