{
  "$schema": "../overlay.schema.json",
  "id": "okf_commaseparatedvalues",
  "kind": "filter",
  "name": "Comma-Separated Values Filter",
  "aliasOf": "okf_table",
  "overview": "The Comma-Separated Values Filter extracts translatable text from CSV-style files, where each record is a line and fields are separated by a single delimiter character (a comma by default, but configurable to semicolon, pipe, or any other character). Field values may be wrapped in a text qualifier so that the delimiter, line breaks, and the qualifier itself can appear inside a value. The filter handles multi-line quoted fields, two qualifier-escaping conventions, and optional headers, and shares the table family's column-role model for selecting source, target, ID, and comment columns. The `format.*` parameters below are specific to CSV; the column, header, and whitespace settings are inherited from the table filter.",
  "limitations": [
    "Column roles are addressed by 1-based index, not by header name."
  ],
  "processingNotes": [
    "Fields are split on the single `format.fieldDelimiter` character; an empty delimiter falls back to treating the whole line as one cell.",
    "A qualified value whose opening qualifier is not closed on the same line triggers multi-line merging: subsequent lines are folded into the field until the qualifier balances.",
    "Embedded line breaks inside a quoted field are reproduced according to `wrapMode`; line breaks between records are preserved.",
    "Two adjacent qualifiers inside a value (or a backslash-escaped qualifier) are moved to the skeleton as an inline code, so the literal qualifier survives the round-trip."
  ],
  "examples": [
    {
      "title": "CSV with a header row",
      "description": "Standard CSV: column names on line 1, data from line 2, comma delimiter, double-quote qualifier. By default columns are extracted as listed (source column 1, target column 2).",
      "config": "```yaml\nconfigId: okf_table_csv\nformat:\n  fieldDelimiter: \",\"\n  textQualifier: '\"'\ncolumnNamesLineNum: 1\nvaluesStartLineNum: 2\nsendHeaderMode: 1\n```",
      "input": "```csv\nName,Description\n\"Widget\",\"A small, useful device\"\n```"
    },
    {
      "title": "Semicolon-delimited, no header",
      "description": "Read a file whose fields are separated by semicolons and whose data starts on the first line.",
      "config": "```yaml\nformat:\n  fieldDelimiter: \";\"\nvaluesStartLineNum: 1\ncolumnNamesLineNum: 0\n```"
    },
    {
      "title": "Qualifier with embedded delimiter and escaped quote",
      "description": "The text qualifier lets a field hold the delimiter; a literal qualifier inside the value is escaped by duplication (`\"\"`) under the default escaping mode.",
      "input": "```csv\nName,Description\n\"Quote Test\",\"She said \"\"hello\"\"\"\n```"
    }
  ],
  "parameters": {
    "format.fieldDelimiter": {
      "help": "The character that separates fields within a row. Any single character may be used — comma for CSV, semicolon and pipe being common alternatives. When empty, the line is not split and the whole line is treated as a single cell.",
      "values": "Single character (default `,`)",
      "examples": [ "`;` — semicolon-delimited", "`|` — pipe-delimited" ]
    },
    "format.textQualifier": {
      "help": "The character placed before and after a field value so the value may contain the field delimiter, line breaks, or the qualifier itself. Qualified content is detected during parsing; whether the qualifier characters are kept in the extracted text depends on `format.removeQualifiers`.",
      "values": "Single character (default `\"`)"
    },
    "format.escapingMode": {
      "help": "How a literal text qualifier embedded in a field value is escaped in the source. Duplication treats two consecutive qualifiers as one literal qualifier (the common CSV convention, e.g. `\"\"`). Backslash treats a backslash before the qualifier as the escape (e.g. `\\\"`). This setting governs both how qualified fields are detected and how escaped qualifiers are recognised and preserved.",
      "values": "`1` = duplication (`\"\"`), `2` = backslash (`\\\"`) (default `1`)"
    },
    "format.removeQualifiers": {
      "help": "When enabled (the default), the surrounding text qualifiers are stripped from extracted values and kept in the skeleton, so translators see only the field content. When disabled, the qualifiers remain part of the extracted text.",
      "values": "`true` / `false` (default `true`)"
    },
    "format.addQualifiers": {
      "help": "When enabled, the writer adds text qualifiers around output values that require them — values containing the delimiter, line breaks, or the qualifier — even if the corresponding source field was not qualified. Use it to guarantee well-formed CSV when translations introduce delimiters or newlines.",
      "values": "`true` / `false` (default `false`)"
    },
    "columnNamesLineNum": {
      "help": "The 1-based line number holding the column captions. For CSV the names are read by splitting that line on the delimiter and stripping qualifiers; they label comment metadata and may set the pipeline locale when a column name is a locale code. Use `0` when the file has no column-name line.",
      "values": "1-based line number; `0` = none (default `0`)"
    },
    "valuesStartLineNum": {
      "help": "The 1-based line where data rows begin. Lines before it form the header area, handled per `sendHeaderMode`. For a header on line 1, set this to 2.",
      "values": "1-based line number (default `1`)"
    },
    "detectColumnsMode": {
      "help": "How the column count per row is decided: per-row, from the number of names on the column-names line, or fixed. CSV rows already split cleanly on the delimiter, so this mainly matters when you need a guaranteed column count.",
      "values": "`0` = none, `1` = from column names, `2` = fixed (default `0`)"
    },
    "sendHeaderMode": {
      "help": "Which header-area lines are extracted as translatable text: none, the column-names line only, or all header lines.",
      "values": "`0` = none, `1` = column names only, `2` = all (default `0`)"
    },
    "sendColumnsMode": {
      "help": "Which columns of each data row are extracted: none, only the columns named in the column-role settings, or all columns as source. `listed` mode enables bilingual extraction, IDs, and notes.",
      "values": "`0` = none, `1` = listed, `2` = all (default `1`)"
    },
    "recordIdColumn": {
      "help": "The 1-based index of the column whose value identifies the row. Combined with `sourceIdSuffixes` to synthesise an ID for source columns lacking an explicit ID cell.",
      "values": "1-based column index; `0` = none (default `0`)"
    },
    "sourceColumns": {
      "help": "Comma-separated 1-based indices of the columns extracted as source (translatable) text. Consulted when `sendColumnsMode` is `listed`.",
      "values": "e.g. `1` or `1,3` (default `1`)"
    },
    "targetColumns": {
      "help": "Comma-separated 1-based indices of columns holding existing target text. Each is tied to a source column via `targetSourceRefs` and a locale via `targetLanguages`; supplying them makes the configuration bilingual.",
      "values": "Comma-separated 1-based indices (default `2`)"
    },
    "targetLanguages": {
      "help": "Locale of each target column, aligned position-by-position with `targetColumns`. A single value applies to all target columns; otherwise the pipeline target locale is used.",
      "values": "Comma-separated locale codes, e.g. `fr,de` (default empty)"
    },
    "commentColumns": {
      "help": "Comma-separated 1-based indices of columns whose values attach as notes (or metadata, see `exposeCommentsAsMetadata`) to the source column they annotate via `commentSourceRefs`, rather than being extracted as translatable text.",
      "values": "Comma-separated 1-based indices (default empty)"
    },
    "sourceIdColumns": {
      "help": "Comma-separated 1-based indices of columns whose values become the resource names (IDs) of source text units, linked to their source column via `sourceIdSourceRefs`. An empty ID cell falls back to `recordIdColumn` + `sourceIdSuffixes`.",
      "values": "Comma-separated 1-based indices (default empty)"
    },
    "commentSourceRefs": {
      "help": "Maps each column in `commentColumns` to the source column it annotates, as a comma-separated list of 1-based source-column indices aligned position-by-position. The comment becomes a read-only note on that source text unit.",
      "values": "Comma-separated 1-based indices (default empty)"
    },
    "sourceIdSourceRefs": {
      "help": "Maps each column in `sourceIdColumns` to the source column it names, as a comma-separated list of 1-based source-column indices aligned position-by-position.",
      "values": "Comma-separated 1-based indices (default empty)"
    },
    "sourceIdSuffixes": {
      "help": "Suffixes appended to the `recordIdColumn` value to build an ID for a source column that has no explicit ID cell. Aligned position-by-position with `sourceColumns`.",
      "values": "Comma-separated strings (default empty)"
    },
    "targetSourceRefs": {
      "help": "Maps each column in `targetColumns` to the source column it translates, as a comma-separated list of 1-based source-column indices aligned position-by-position. A target whose reference cannot be resolved is sent to the skeleton.",
      "values": "Comma-separated 1-based indices (default `1`)"
    },
    "exposeCommentsAsMetadata": {
      "help": "When enabled, comment-column values attach to the source text unit as metadata keyed by column name instead of as translator notes.",
      "values": "`true` / `false` (default `false`)",
      "dependsOn": [
        { "property": "commentColumns", "condition": "only meaningful when comment columns are defined" }
      ]
    },
    "preserveWS": {
      "help": "Inherited from the plain-text base. When enabled, whitespace inside extracted values is preserved; cell-edge trimming is governed by `trimMode`, `trimLeading`, and `trimTrailing`.",
      "values": "`true` / `false` (default `true`)"
    },
    "trimMode": {
      "help": "How leading/trailing whitespace is removed from cells: none, non-qualified fields only (leaving the inside of quoted fields intact), or all fields. Any non-none value forces `trimLeading` and `trimTrailing` on. The non-qualified option is meaningful for CSV because it distinguishes quoted from unquoted fields.",
      "values": "`0` = none, `1` = non-qualified only, `2` = all (default `1`)"
    },
    "trimLeading": {
      "help": "Removes leading spaces and tabs from each extracted cell value. Forced on whenever `trimMode` is not none.",
      "values": "`true` / `false` (default `true`)"
    },
    "trimTrailing": {
      "help": "Removes trailing spaces and tabs from each extracted cell value. Forced on whenever `trimMode` is not none.",
      "values": "`true` / `false` (default `true`)"
    },
    "unescapeSource": {
      "help": "Inherited from the plain-text base. When enabled, `\\t`, `\\n`, `\\\\`, and `\\uXXXX` escape sequences in extracted values are converted to the characters they represent.",
      "values": "`true` / `false` (default `true`)"
    },
    "wrapMode": {
      "help": "How a value spanning multiple physical lines (a quoted multi-line field) is represented: `NONE` joins wrapped lines with a line feed, `SPACES` joins with a single space, `PLACEHOLDERS` inserts an inline placeholder at each internal break so the wrapping can be reproduced on output.",
      "values": "`NONE`, `SPACES`, `PLACEHOLDERS` (default `NONE`)"
    }
  },
  "wikiUrl": "https://okapiframework.org/wiki/index.php/Table_Filter",
  "x-provenance": {
    "generator": "claude-opus-4-7",
    "grounding": [
      "okapi-source:okapi/filters/table/src/main/java/net/sf/okapi/filters/table/csv/Parameters.java",
      "okapi-source:okapi/filters/table/src/main/java/net/sf/okapi/filters/table/csv/CommaSeparatedValuesFilter.java",
      "okapi-source:okapi/filters/table/src/main/java/net/sf/okapi/filters/table/base/Parameters.java",
      "okapi-source:okapi/filters/table/src/main/java/net/sf/okapi/filters/table/base/BaseTableFilter.java",
      "schema"
    ],
    "authoredAt": "2026-05-23",
    "reviewed": false
  }
}
