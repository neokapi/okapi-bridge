{
  "$schema": "../overlay.schema.json",
  "id": "okf_openoffice",
  "kind": "filter",
  "name": "OpenOffice / ODF Package Filter",
  "aliasOf": "okf_odf",
  "overview": "The OpenOffice filter processes OpenDocument packages — the ZIP archives produced by OpenOffice.org and LibreOffice: text (`.odt`), spreadsheet (`.ods`), presentation (`.odp`), graphics (`.odg`), and their template variants. It is the package-level companion to the ODF filter: it opens the ZIP, walks each embedded sub-document (`content.xml`, `meta.xml`, `styles.xml`), and delegates the inner OpenDocument XML to the ODF filter for extraction. The translated sub-documents are re-zipped so the output is a valid OpenDocument package.\n\nUse this configuration when the input is a complete `.odt`/`.ods`/`.odp`/`.odg` file. To process a raw, unpackaged OpenDocument XML stream directly, use the ODF filter instead. Both share the same parameters, so the extraction and output options behave identically.",
  "limitations": [
    "Input must be a ZIP-packaged OpenDocument file; raw XML streams should be processed with the ODF filter."
  ],
  "processingNotes": [
    "Each entry in the ZIP package is processed as a separate sub-document; in XLIFF output each becomes its own `<file>`.",
    "Typically only `content.xml` yields extracted text; the other sub-documents are often empty.",
    "The translated sub-documents are repackaged into a ZIP archive on output."
  ],
  "examples": [
    {
      "title": "Keep special glyphs literal in page-number and reference text",
      "description": "Disable entity encoding so character data inside the read-only inline elements that carry glyphs (page numbers, bookmark references) is passed through verbatim rather than escaped to character entity references on output.",
      "config": "```yaml\nextraction:\n  encodeCharacterEntityReferenceGlyphs: false\n```"
    }
  ],
  "parameters": {
    "extraction.encodeCharacterEntityReferenceGlyphs": {
      "help": "Writer-side option affecting only the read-only inline elements that carry encodable glyphs — `text:page-number` and `text:bookmark-ref`. When enabled (the default), the character data inside those elements is run through the XML encoder, so reserved characters such as `&`, `<`, and `>` are written as character entity references. When disabled, that text is passed through literally. Other elements are unaffected. Behaviour is identical to the ODF filter, since the package filter delegates inner-XML extraction to it.",
      "values": "`true` / `false` (default `true`)"
    }
  },
  "wikiUrl": "https://okapiframework.org/wiki/index.php/OpenOffice_Filter",
  "x-provenance": {
    "generator": "claude-opus-4-7",
    "grounding": [
      "okapi-source:okapi/filters/openoffice/src/main/java/net/sf/okapi/filters/openoffice/OpenOfficeFilter.java",
      "okapi-source:okapi/filters/openoffice/src/main/java/net/sf/okapi/filters/openoffice/Parameters.java",
      "okapi-source:okapi/filters/openoffice/src/main/java/net/sf/okapi/filters/openoffice/ODFFilter.java",
      "wiki:https://okapiframework.org/wiki/index.php/OpenOffice_Filter",
      "schema"
    ],
    "authoredAt": "2026-05-23",
    "reviewed": false
  }
}
