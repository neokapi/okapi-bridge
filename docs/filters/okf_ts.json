{
  "filterName": "TS Filter",
  "overview": "The TS Filter processes **Qt TS (Translation Source)** resource files, implementing the `IFilter` interface. It is based on the [TS specification](http://doc.trolltech.com/4.3/linguist-ts-file-format.html) from Qt/Trolltech. The filter handles extraction of translatable content from `<translation>` elements, mapping the `type` attribute to approval states, and supports inline code detection via configurable regular expressions.",
  "parameters": {
    "output.escapeGT": {
      "help": "Controls whether the `>` character is escaped as `&gt;` in the output TS file. By default, XML only requires escaping `<` and `&`, but some tools or workflows may expect `>` to also be escaped."
    },
    "output.decodeByteValues": {
      "help": "Converts `<byte value=\"[value]\"/>` elements in the TS document to their raw character equivalents.\n\nTS files use `<byte/>` elements to represent characters that are invalid in XML. If left disabled, these elements are preserved as **inline codes**.\n\n**Recommendation:** Keep disabled if your downstream workflow uses XML-based formats like XLIFF or TMX, since the raw characters may be invalid in those formats as well.",
      "notes": [
        "Characters stored as `<byte/>` elements are invalid in XML — converting them to raw characters may cause issues in XML-based interchange formats like XLIFF or TMX."
      ]
    },
    "inlineCodes": {
      "help": "Defines regular expressions to detect inline codes within extracted text. Any match is converted to an inline code placeholder.\n\nThe default expression captures:\n- **printf-style format specifiers**: `%d`, `%s`, `%2$s`, `%-10.2f`, etc.\n- **Escape sequences**: `\\n`, `\\r\\n`, `\\t`, `\\a`, `\\b`, `\\f`, `\\v`\n- **Numbered placeholders**: `{0}`, `{1 something}`, etc.",
      "examples": [
        "`%d`, `%s`, `%2$s` — printf-style format specifiers",
        "`\\n`, `\\r\\n`, `\\t` — escape sequences",
        "`{0}`, `{1}` — numbered placeholders"
      ]
    }
  },
  "processingNotes": [
    "Line-break style in the output matches the original input file (no normalization).",
    "The `<translation>` element's `type` attribute controls extraction behavior: no attribute → approved (`yes`), `unfinished` → not approved (`no`), `obsolete` → **not extracted** (treated as unused entries).",
    "The `<translatorcomment>` element is stored as a `Property.TRANSNOTE` on the text unit, accessible by subsequent pipeline steps or editors."
  ],
  "limitations": [
    "There are sometimes issues with extracting and merging back `<numerusform>` elements. Perform a **round-trip test** before sending for translation."
  ],
  "examples": [
    {
      "title": "Default inline code pattern",
      "description": "The default regex pattern used for detecting inline codes in TS content. It matches printf-style placeholders, escape sequences, and numbered placeholders.",
      "config": "```regex\n((%(([-0+#]?)[-0+#]?)((\\d\\$)?)([\\d\\*]*(\\.[\\d\\*]*)?)[dioxXucsfeEgGpn])\n|((\\\\r\\\\n)|\\\\a|\\\\b|\\\\f|\\\\n|\\\\r|\\\\t|\\\\v)\n|(\\{\\d.*?\\}))\n```"
    },
    {
      "title": "Translation type mapping",
      "description": "Shows how the `type` attribute on `<translation>` elements maps to extraction behavior.",
      "input": "```xml\n<message>\n  <source>Hello</source>\n  <translation>Bonjour</translation>\n</message>\n<message>\n  <source>World</source>\n  <translation type=\"unfinished\">Monde</translation>\n</message>\n<message>\n  <source>Old text</source>\n  <translation type=\"obsolete\">Ancien texte</translation>\n</message>\n```",
      "output": "```text\nFirst message: extracted, target approved = yes\nSecond message: extracted, target approved = no\nThird message: NOT extracted (obsolete)\n```"
    }
  ],
  "filterId": "okf_ts",
  "wikiUrl": "https://okapiframework.org/wiki/index.php/TS_Filter"
}
