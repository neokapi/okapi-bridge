{
  "filterName": "XML Filter",
  "overview": "The XML Filter processes XML documents using a DOM-based parser with full **ITS (Internationalization Tag Set)** support (both 1.0 and 2.0). By default, all element content is translatable and no attribute values are translatable — behavior is customized via ITS rules in a parameters file (`.fprm`). If you need to process very large XML documents and have no need for ITS, consider using the **XML Stream Filter** instead.",
  "parameters": {
    "lineBreakAsCode": {
      "help": "Treats line-break characters (`&#10;`) in element content as inline codes rather than literal line breaks in the extracted text.\n\nUseful for XML documents generated by Excel where cell formatting uses `&#10;` entity references — these need to be passed as inline codes rather than breaking the text into multiple lines.\n\nAffects **all** extracted content globally.",
      "values": "`yes` or `no` (default: `no`)",
      "notes": [
        "This option must be placed in the parameters file (`.fprm`), not in embedded or linked ITS rules. Options in embedded/linked ITS rules have no effect."
      ]
    },
    "codeFinder": {
      "help": "Defines regular expressions to capture spans of extracted text that should be treated as inline codes. Useful when element content contains variables (e.g. `#VAR#`), placeholders (e.g. `%1%`), or embedded HTML tags (e.g. `<b>`) that must be protected from modification.\n\nThe code finder uses a specific serialization format inside the `<okp:codeFinder>` element:\n\n```xml\n<okp:codeFinder useCodeFinder=\"yes\">#v1\ncount.i=2\nrule0=&lt;(/?)\\ w[^&lt;]*?&gt;\nrule1=(#\\w+?\\#)|(%\\d+?%)\n</okp:codeFinder>\n```\n\n**Format rules:**\n- Set `useCodeFinder=\"yes\"` to activate (defaults to `no` if missing)\n- First line of content must be `#v1`\n- `count.i=N` must precede rules, where `N` is the total rule count\n- Rules are numbered `rule0`, `rule1`, etc.\n- Patterns must be **XML-escaped** (e.g. `<` → `&lt;`, `>` → `&gt;`)\n- No leading spaces before `count.i` or `ruleN`, no trailing spaces after expressions",
      "values": "Serialized `#v1` code finder format with Java regex patterns (XML-escaped)",
      "notes": [
        "This option must be placed in the parameters file (`.fprm`), not in embedded or linked ITS rules.",
        "Regex patterns must be escaped for XML — `<(/?)\\ w[^>]*?>` becomes `&lt;(/?)\\ w[^&lt;]*?&gt;` in the parameters file."
      ],
      "examples": [
        "`&lt;(/?)\\ w[^&lt;]*?&gt;` — matches XML-type tags like `<b>`, `</b>`, `<br/>`",
        "`(#\\w+?\\#)|(%\\d+?%)` — matches `#VAR#` or `%1%` style placeholders"
      ],
      "seeAlso": "XML_Filter#codeFinder"
    },
    "omitXMLDeclaration": {
      "help": "Suppresses the XML declaration (`<?xml ...?>`) from the output document.\n\nBy default an XML declaration is always written at the top of the output, regardless of whether the original had one. This is needed when the output encoding is not UTF-8, UTF-16, or UTF-32. Only use this option for rare special cases where the declaration must be omitted.",
      "values": "`yes` or `no` (default: `no`)",
      "notes": [
        "XML documents without a declaration may be read incorrectly if the encoding is not UTF-8, UTF-16, or UTF-32.",
        "This option must be placed in the parameters file (`.fprm`), not in embedded or linked ITS rules."
      ]
    },
    "escapeQuotes": {
      "help": "Controls whether double-quote characters (`\"`) are escaped in **element content**.\n\nDefault escaping behavior:\n- **Attribute values**: single-quotes never escaped, double-quotes always escaped (cannot be changed)\n- **Element content**: single-quotes not escaped, double-quotes escaped by default\n\nThis option **only takes effect** if no ITS rule triggers translation of any attribute in the document. If any attribute is made translatable, this option is ignored and double-quotes are always escaped in element content.",
      "values": "`yes` or `no` (default: `yes`)",
      "notes": [
        "Only affects element content, not attribute values.",
        "Ignored when any translatable attribute rule is triggered in the document.",
        "This option must be placed in the parameters file (`.fprm`)."
      ]
    },
    "escapeGT": {
      "help": "Controls whether the greater-than character (`>`) is escaped as `&gt;` in the output.",
      "values": "`yes` or `no` (default: `yes`)",
      "notes": [
        "This option must be placed in the parameters file (`.fprm`)."
      ]
    },
    "escapeNbsp": {
      "help": "Controls whether the non-breaking space character is escaped as `&#x00a0;` in the output.",
      "values": "`yes` or `no` (default: `yes`)",
      "notes": [
        "This option must be placed in the parameters file (`.fprm`)."
      ]
    },
    "extractIfOnlyCodes": {
      "help": "Controls whether entries containing only white-space and/or inline codes (no actual translatable text) are extracted.\n\nBy default all extractable entries are extracted even when they contain only whitespace and/or inline codes. Set to `no` to skip such entries.",
      "values": "`yes` (default) or `no`",
      "notes": [
        "This option must be placed in the parameters file (`.fprm`)."
      ]
    },
    "inlineCdata": {
      "help": "Controls how CDATA sections are handled. By default, CDATA sections are exposed as regular content and the CDATA markers (`<![CDATA[` and `]]>`) are discarded. When enabled, the CDATA markers are exposed as inline codes instead.",
      "values": "`yes` or `no` (default: `no`)",
      "notes": [
        "This option must be placed in the parameters file (`.fprm`)."
      ]
    },
    "extractUntranslatable": {
      "help": "Extracts entries marked as untranslatable (`its:translate=\"no\"`) alongside translatable content. Extracted untranslatable entries are marked as `translate=\"no\"` in the output (e.g. XLIFF).\n\nUseful for extracting context around translatable text.\n\n**Tip:** To selectively exclude certain untranslatable elements from extraction, combine with the `localeFilterRule` and set `localeFilterList=\"!*\"` on elements to skip:\n\n```xml\n<its:localeFilterRule selector=\"//yourTag\" localeFilterList=\"!*\"/>\n```",
      "values": "`yes` or `no` (default: `no`)",
      "notes": [
        "This option must be placed in the parameters file (`.fprm`).",
        "When enabled, **all** untranslatable content is extracted by default. Use `localeFilterList=\"!*\"` to selectively exclude specific elements."
      ]
    }
  },
  "limitations": [
    "The ITS rule `withinTextRule` with value `nested` may act like `yes` in some cases.",
    "The values of `xml:lang` attributes are **not** updated in output to reflect the target language.",
    "The entire input file is loaded into memory (DOM parser) — very large documents may cause memory issues. Consider the **XML Stream Filter** for large files."
  ],
  "processingNotes": [
    "**Input encoding**: Uses the document's encoding declaration if present; otherwise defaults to UTF-8 (ignores any externally specified default encoding).",
    "**Output encoding (UTF-8)**: BOM is written only if the input was UTF-8 *and* had a BOM. If converting from another encoding to UTF-8, no BOM is added.",
    "**XML declaration**: If the original document had an encoding declaration it is updated; if not, one is automatically added (unless `omitXMLDeclaration` is set).",
    "**Line-breaks**: Output preserves the same line-break style as the input.",
    "**ITS rule precedence**: External parameters file rules → document global rules → document local rules (later rules override earlier ones).",
    "**DOM-based parser**: The entire document is loaded into memory, enabling full ITS support but limiting maximum file size."
  ],
  "examples": [
    {
      "title": "Basic ITS rules file with filter options",
      "description": "A parameters file (`.fprm`) that marks `<head>` as non-translatable, treats `<b>`, `<code>`, and `<img>` as inline elements, and enables line-break-as-code processing.",
      "config": "```xml\n<its:rules version=\"1.0\"\n xmlns:its=\"http://www.w3.org/2005/11/its\"\n xmlns:okp=\"okapi-framework:xmlfilter-options\">\n <its:translateRule selector=\"//head\" translate=\"no\"/>\n <its:withinTextRule selector=\"//b|//code|//img\" withinText=\"yes\"/>\n <okp:options lineBreakAsCode=\"yes\"/>\n</its:rules>\n```"
    },
    {
      "title": "Combined filter options",
      "description": "Multiple filter options must be set in a single `<okp:options>` element.",
      "config": "```xml\n<its:rules version=\"1.0\"\n xmlns:its=\"http://www.w3.org/2005/11/its\"\n xmlns:okp=\"okapi-framework:xmlfilter-options\">\n <okp:options lineBreakAsCode=\"yes\"\n              escapeQuotes=\"no\"\n              escapeGT=\"yes\"/>\n</its:rules>\n```"
    },
    {
      "title": "Code finder with regex patterns",
      "description": "Defines regex rules to treat embedded HTML tags and placeholder variables as inline codes.",
      "config": "```xml\n<its:rules version=\"1.0\"\n xmlns:its=\"http://www.w3.org/2005/11/its\"\n xmlns:okp=\"okapi-framework:xmlfilter-options\">\n <okp:codeFinder useCodeFinder=\"yes\">#v1\ncount.i=2\nrule0=&lt;(/?)\\ w[^&lt;]*?&gt;\nrule1=(#\\w+?\\#)|(%\\d+?%)\n </okp:codeFinder>\n</its:rules>\n```"
    },
    {
      "title": "Custom ID values with ITS extensions",
      "description": "Uses the `itsx:idValue` extension to derive text unit IDs from XPath expressions on element attributes.",
      "config": "```xml\n<its:rules version=\"1.0\" xmlns:its=\"http://www.w3.org/2005/11/its\"\n xmlns:itsx=\"http://www.w3.org/2008/12/its-extensions\">\n <its:translateRule selector=\"//text\" translate=\"yes\" itsx:idValue=\"concat(../@name, '_t')\"/>\n <its:translateRule selector=\"//desc\" translate=\"yes\" itsx:idValue=\"concat(../@name, '_d')\"/>\n</its:rules>\n```",
      "input": "```xml\n<doc>\n <msg name=\"id1\">\n  <text>Value of text</text>\n  <desc>Value of desc</desc>\n </msg>\n</doc>\n```",
      "output": "```\nText unit \"id1_t\": \"Value of text\"\nText unit \"id1_d\": \"Value of desc\"\n```"
    },
    {
      "title": "Extracting untranslatable content with selective exclusion",
      "description": "Extracts untranslatable entries for context, while excluding specific elements using `localeFilterList`.",
      "config": "```xml\n<its:rules version=\"1.0\"\n xmlns:its=\"http://www.w3.org/2005/11/its\"\n xmlns:okp=\"okapi-framework:xmlfilter-options\">\n <okp:options extractUntranslatable=\"yes\"/>\n <its:localeFilterRule selector=\"//metadata\" localeFilterList=\"!*\"/>\n</its:rules>\n```"
    }
  ],
  "fullDoc": "# XML Filter\n\nThe XML Filter processes XML documents using a DOM-based parser with full **ITS (Internationalization Tag Set)** support (both 1.0 and 2.0). By default, all element content is translatable and no attribute values are translatable — behavior is customized via ITS rules in a parameters file (`.fprm`). If you need to process very large XML documents and have no need for ITS, consider using the **XML Stream Filter** instead.\n\n## Parameters\n\nThis filter stores its parameters in an ITS XML document (`.fprm` file). There is no graphical editor — edit the file with a text or XML editor.\n\n### ITS Support\n\nBy default the filter processes XML documents based on **ITS defaults**:\n- The content of all elements is translatable\n- None of the attribute values are translatable\n\nBehavior is customized through ITS rules which are applied in this order (later rules override earlier):\n1. External parameters file (`.fprm`) global rules\n2. Document-embedded global rules\n3. Document local rules\n\nThe filter supports ITS 1.0 and ITS 2.0 (backward compatible).\n\n### ITS Extensions\n\nExtensions use the namespace `http://www.w3.org/2008/12/its-extensions`.\n\n#### idValue and xml:id\n\n> **Note:** This extension was defined for ITS 1.0. ITS 2.0 offers the new **Id Value** data category that should be used instead.\n\nWhen `xml:id` is found on a translatable element, it is used as the text unit name. The `itsx:idValue` attribute in `translateRule` allows defining an XPath expression for the identifier. `xml:id` takes precedence over `idValue`.\n\n#### whiteSpaces\n\n> **Note:** This extension was defined for ITS 1.0. ITS 2.0 offers the new **Preserve Space** data category that should be used instead.\n\nThe `itsx:whiteSpaces=\"preserve\"` attribute applies the equivalent of `xml:space=\"preserve\"` globally via ITS rules. The `xml:space` attribute takes precedence.\n\n### Filter Options\n\nFilter options use the namespace `okapi-framework:xmlfilter-options`. **All options must be placed in the parameters file (`.fprm`), not in embedded or linked ITS rules.** Multiple options must be in a single `<okp:options>` element.\n\n#### lineBreakAsCode\n\nTreats line-break characters (`&#10;`) in element content as inline codes rather than literal line breaks. Useful for Excel-generated XML where `&#10;` entity references mark cell formatting. Affects all extracted content globally.\n\nDefault: `no`\n\n#### codeFinder\n\nDefines regular expressions to capture spans of extracted text that should be treated as inline codes. Uses a specific `#v1` serialization format. Set `useCodeFinder=\"yes\"` to activate. Patterns must be XML-escaped.\n\n> **Note:** Regex patterns must be escaped for XML — `<(/?)\\w[^>]*?>` becomes `&lt;(/?)\\w[^&lt;]*?&gt;`.\n\n#### omitXMLDeclaration\n\nSuppresses the XML declaration from the output. By default a declaration is always written.\n\nDefault: `no`\n\n> **Warning:** XML documents without a declaration may be read incorrectly if the encoding is not UTF-8, UTF-16, or UTF-32.\n\n#### escapeQuotes\n\nControls whether double-quote characters are escaped in element content. Only takes effect if no ITS rule triggers translation of any attribute in the document.\n\nDefault: `yes`\n\n#### escapeGT\n\nControls whether `>` is escaped as `&gt;` in the output.\n\nDefault: `yes`\n\n#### escapeNbsp\n\nControls whether the non-breaking space character is escaped as `&#x00a0;`.\n\nDefault: `yes`\n\n#### extractIfOnlyCodes\n\nControls whether entries containing only whitespace and/or inline codes are extracted.\n\nDefault: `yes`\n\n#### inlineCdata\n\nWhen enabled, CDATA markers are exposed as inline codes instead of being discarded.\n\nDefault: `no`\n\n#### extractUntranslatable\n\nExtracts entries marked `its:translate=\"no\"` for context. Extracted entries are marked `translate=\"no\"` in XLIFF output. Use `localeFilterList=\"!*\"` to selectively exclude specific elements.\n\nDefault: `no`\n\n## Limitations\n\n- The ITS rule `withinTextRule` with value `nested` may act like `yes` in some cases.\n- `xml:lang` attribute values are **not** updated in output to reflect the target language.\n- The entire input file is loaded into memory (DOM parser) — very large documents may cause memory issues.\n\n## Notes\n\n- **Input encoding**: Uses the document's encoding declaration if present; otherwise defaults to UTF-8.\n- **Output encoding (UTF-8)**: BOM is written only if input was UTF-8 and had a BOM.\n- **XML declaration**: Automatically added or updated unless `omitXMLDeclaration` is set.\n- **Line-breaks**: Output preserves the same line-break style as the input.\n- **ITS rule precedence**: External parameters → document global → document local (later overrides earlier).\n\n## Examples\n\n### Basic ITS Parameters File\n\n```xml\n<its:rules version=\"1.0\"\n xmlns:its=\"http://www.w3.org/2005/11/its\"\n xmlns:okp=\"okapi-framework:xmlfilter-options\">\n <its:translateRule selector=\"//head\" translate=\"no\"/>\n <its:withinTextRule selector=\"//b|//code|//img\" withinText=\"yes\"/>\n <okp:options lineBreakAsCode=\"yes\" escapeQuotes=\"no\"/>\n</its:rules>\n```\n\n### Code Finder with Regex Patterns\n\n```xml\n<okp:codeFinder useCodeFinder=\"yes\">#v1\ncount.i=2\nrule0=&lt;(/?)\\w[^&lt;]*?&gt;\nrule1=(#\\w+?\\#)|(%\\d+?%)\n</okp:codeFinder>\n```\n\n### Custom ID Values\n\n```xml\n<its:translateRule selector=\"//text\" translate=\"yes\" itsx:idValue=\"concat(../@name, '_t')\"/>\n<its:translateRule selector=\"//desc\" translate=\"yes\" itsx:idValue=\"concat(../@name, '_d')\"/>\n```",
  "filterId": "okf_xml",
  "wikiUrl": "https://okapiframework.org/wiki/index.php/XML_Filter"
}
