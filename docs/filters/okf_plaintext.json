{
  "filterName": "Plain Text Filter",
  "overview": "The Plain Text filter processes text files encoded in ANSI, Unicode, UTF-8, and UTF-16 with automatic Byte-Order-Mark detection. It provides three extraction modes: **line-based** (`okf_baseplaintext`), **paragraph-based** (`okf_paraplaintext`), and **regex-based** (`okf_regexplaintext`), plus a **spliced lines** variant (`okf_splicedlines`) for source code with line-continuation characters. Each mode shares common parser and inline code settings but exposes different extraction parameters.",
  "parameters": {
    "parser.trimLeading": {
      "help": "Strips leading spaces and tabs from each extracted text unit. Useful for indented source files where leading whitespace is not part of the translatable content.",
      "notes": [
        "For CSV-type usage, this works together with the CSV \"Exclude leading/trailing white spaces\" option."
      ]
    },
    "parser.trimTrailing": {
      "help": "Strips trailing spaces and tabs from each extracted text unit. Commonly used to clean up lines with trailing whitespace before translation.",
      "notes": [
        "For CSV-type usage, this works together with the CSV \"Exclude leading/trailing white spaces\" option."
      ]
    },
    "parser.unescapeSource": {
      "help": "Converts escape sequences (`\\t`, `\\n`, `\\\\`, `\\uXXXX`) in the source text into their corresponding characters before extraction. Disable if the file contains literal backslash sequences that should be preserved as-is."
    },
    "parser.preserveWS": {
      "help": "Preserves original whitespace in extracted text units. Typically enabled for paragraph and regex modes where whitespace structure matters."
    },
    "parser.wrapMode": {
      "help": "Controls how multiple lines are combined into a single text unit when extracting multi-line content:\n\n- **NONE** — Lines are separated by `\\n` line feeds (preserves original line structure)\n- **SPACES** — Lines are unwrapped and merged with a space between them (useful for reflowable text)\n- **PLACEHOLDERS** — Lines are joined with an inline code placeholder representing the original line break (allows translators to reposition breaks)",
      "values": "`NONE` = separate with line feeds, `SPACES` = unwrap/merge with spaces, `PLACEHOLDERS` = inline code placeholders for line breaks"
    },
    "extraction.extractParagraphs": {
      "help": "Groups consecutive non-empty lines into paragraph text units, separated by one or more blank lines. Each paragraph becomes a single translatable unit with internal line breaks handled according to `wrapMode`.\n\nWhen disabled, each line becomes its own text unit.",
      "notes": [
        "Only available in the paragraph-based sub-filter (`okf_paraplaintext`)."
      ]
    },
    "extraction.rule": {
      "help": "Java regular expression used to extract translatable text. The filter applies this regex to the input and creates text units from the matches. Use capturing groups to isolate the translatable portion, then set `sourceGroup` to the group index.\n\nThe regex is applied with the flags specified in `regexOptions`.",
      "values": "Java `java.util.regex.Pattern` syntax. Use capturing groups `()` to isolate translatable text.",
      "examples": [
        "`(^(?=.+))(.*?)$` — extract each non-empty line (group 2 = content, with MULTILINE flag)",
        "`(\\n*)(.*?)(\\n\\n|\\Z)` — extract paragraphs separated by blank lines (group 2 = paragraph text, with MULTILINE + DOTALL flags)"
      ],
      "seeAlso": "Plain_Text_Filter#Extraction_Rule"
    },
    "extraction.sourceGroup": {
      "help": "The 1-based index of the capturing group in the extraction regex whose match contains the translatable text. Group 0 is the entire match.\n\nFor example, with regex `(prefix)(content)(suffix)`, set to `2` to extract only the `content` group.",
      "values": "Integer index of a capturing group defined in the extraction `rule`. Must be ≥ 0."
    },
    "extraction.regexOptions": {
      "help": "Bitmask of `java.util.regex.Pattern` flags to apply to the extraction regex. Common flags:\n\n- **8** = `MULTILINE` — `^` and `$` match line boundaries (not just start/end of input)\n- **32** = `DOTALL` — `.` matches any character including newline\n- **40** = `MULTILINE | DOTALL` (8 + 32)",
      "values": "Integer bitmask combining `java.util.regex.Pattern` flag constants: `CASE_INSENSITIVE` (2), `MULTILINE` (8), `DOTALL` (32), etc.",
      "examples": [
        "`8` — MULTILINE: for line-by-line extraction",
        "`40` — MULTILINE + DOTALL: for paragraph extraction across line boundaries"
      ]
    },
    "extraction.sample": {
      "help": "Sample text used to test the extraction regex interactively. Extracted matches are shown enclosed in square brackets below the sample. This is for preview/testing only and does not affect processing."
    },
    "extraction.splicer": {
      "help": "The character that indicates a line continues on the next line (line-continuation character). When found at the end of a line, the filter merges that line with the following one into a single text unit.\n\nCommon splicers: `\\` (backslash, used in C/C++/shell), `_` (underscore, used in VB).",
      "values": "A single character. Common values: `\\` (backslash), `_` (underscore), or any custom character.",
      "examples": [
        "`\\` — backslash splicer for C/shell continuation lines",
        "`_` — underscore splicer for Visual Basic continuation lines"
      ],
      "notes": [
        "Only active in the spliced lines sub-filter (`okf_splicedlines`)."
      ]
    },
    "extraction.createPlaceholders": {
      "help": "Creates an inline code placeholder at each position where a splicer character was removed during line merging. This preserves the original line break positions in the translatable content, allowing translators to see where breaks occurred.",
      "notes": [
        "Only active in the spliced lines sub-filter (`okf_splicedlines`)."
      ]
    },
    "inlineCodes.enabled": {
      "help": "Activates regex-based detection of inline codes within extracted text. Any text matching the defined patterns will be converted to inline code placeholders, protecting them from translation.",
      "seeAlso": "Plain_Text_Filter#Inline_Codes"
    },
    "inlineCodes.rules": {
      "help": "Define regex patterns to identify inline codes (placeholders, format specifiers, escape sequences, etc.) within translatable text. Each pattern match is converted to an inline code placeholder.\n\nDefault rules typically match C-style format specifiers (`%d`, `%s`) and escape sequences (`\\n`, `\\t`).",
      "examples": [
        "`%(([-0+#]?)[-0+#]?)((\\\\d\\\\$)?)(([\\\\d\\\\*]*)(\\\\.[\\\\d\\\\*]*)?)` — C printf format specifiers",
        "`(\\\\r\\\\n)|\\\\a|\\\\b|\\\\f|\\\\n|\\\\r|\\\\t|\\\\v` — C escape sequences"
      ]
    }
  },
  "processingNotes": [
    "The filter auto-detects Unicode Byte-Order-Mark (BOM) to determine encoding. If no BOM is found, the configured default encoding is used.",
    "For UTF-8 output: a BOM is written only if the input was UTF-8 *and* had a BOM. Converting from non-UTF-8 to UTF-8 produces no BOM.",
    "Output line-break type preserves the original input's line-break convention (CR, LF, or CRLF).",
    "The regex-based sub-filter (`okf_regexplaintext`) detects a wider range of linebreaks at the cost of lower speed and higher memory usage compared to the line-based sub-filter."
  ],
  "limitations": [],
  "examples": [
    {
      "title": "Line-by-line extraction with trailing trim",
      "description": "Uses the base plain text filter to extract each line as a separate text unit, trimming trailing whitespace.",
      "config": "```json\n{\n  \"parser\": {\n    \"trimLeading\": false,\n    \"trimTrailing\": true,\n    \"unescapeSource\": true,\n    \"wrapMode\": \"NONE\"\n  }\n}\n```"
    },
    {
      "title": "Paragraph extraction",
      "description": "Groups consecutive non-empty lines into paragraph text units. Paragraphs are separated by one or more blank lines.",
      "config": "```json\n{\n  \"extraction\": {\n    \"extractParagraphs\": true\n  },\n  \"parser\": {\n    \"preserveWS\": true,\n    \"wrapMode\": \"NONE\"\n  }\n}\n```",
      "input": "```text\nThis is the first sentence.\nAnd this continues the paragraph.\n\nThis is a new paragraph.\n```",
      "output": "```text\nText Unit 1: \"This is the first sentence.\\nAnd this continues the paragraph.\"\nText Unit 2: \"This is a new paragraph.\"\n```"
    },
    {
      "title": "Regex extraction by line",
      "description": "Uses a regex rule with MULTILINE flag to extract each non-empty line. Group 2 captures the line content.",
      "config": "```json\n{\n  \"extraction\": {\n    \"rule\": \"(^(?=.+))(.*?)$\",\n    \"sourceGroup\": 2,\n    \"regexOptions\": 8\n  }\n}\n```"
    },
    {
      "title": "Spliced lines with backslash continuation",
      "description": "Merges lines ending with backslash into single text units, inserting inline code placeholders where splicers were removed.",
      "config": "```json\n{\n  \"extraction\": {\n    \"splicer\": \"\\\\\",\n    \"createPlaceholders\": true\n  }\n}\n```",
      "input": "```text\nThis line continues \\\non the next line.\nThis is separate.\n```",
      "output": "```text\nText Unit 1: \"This line continues <x/>on the next line.\"\nText Unit 2: \"This is separate.\"\n```"
    }
  ],
  "fullDoc": "# Plain Text Filter\n\nThe Plain Text filter processes text files encoded in ANSI, Unicode, UTF-8, and UTF-16 with automatic Byte-Order-Mark detection. It provides three extraction modes: **line-based** (`okf_baseplaintext`), **paragraph-based** (`okf_paraplaintext`), and **regex-based** (`okf_regexplaintext`), plus a **spliced lines** variant (`okf_splicedlines`) for source code with line-continuation characters. Each mode shares common parser and inline code settings but exposes different extraction parameters.\n\n## Parameters\n\n### Extraction (Paragraph Mode)\n\n#### Extract by Paragraphs (`extraction.extractParagraphs`)\nGroups consecutive non-empty lines into paragraph text units, separated by one or more blank lines. Each paragraph becomes a single translatable unit with internal line breaks handled according to `wrapMode`. When disabled, each line becomes its own text unit.\n\n> Only available in the paragraph-based sub-filter (`okf_paraplaintext`).\n\n### Extraction (Regex Mode)\n\n#### Extraction Rule (`extraction.rule`)\nJava regular expression used to extract translatable text. The filter applies this regex to the input and creates text units from matches. Use capturing groups to isolate the translatable portion, then set `sourceGroup` to the group index.\n\nExamples:\n- `(^(?=.+))(.*?)$` — extract each non-empty line (group 2 = content, with MULTILINE flag)\n- `(\\n*)(.*)(\\n\\n|\\Z)` — extract paragraphs separated by blank lines (group 2, with MULTILINE + DOTALL)\n\n#### Source Group (`extraction.sourceGroup`)\nThe 1-based index of the capturing group in the extraction regex whose match contains the translatable text. Group 0 is the entire match.\n\n#### Regex Options (`extraction.regexOptions`)\nBitmask of `java.util.regex.Pattern` flags:\n- **8** = `MULTILINE` — `^` and `$` match line boundaries\n- **32** = `DOTALL` — `.` matches newlines\n- **40** = `MULTILINE | DOTALL`\n\n#### Sample Text (`extraction.sample`)\nSample text for interactively testing the extraction regex. Extracted matches display in square brackets. Does not affect processing.\n\n### Extraction (Spliced Lines Mode)\n\n#### Splicer Character (`extraction.splicer`)\nThe character at the end of a line indicating continuation on the next line. Common values: `\\` (C/shell), `_` (VB).\n\n#### Create Placeholders for Splicers (`extraction.createPlaceholders`)\nCreates inline code placeholders where splicer characters were removed, preserving original line break positions.\n\n### Parser Settings\n\n#### Trim Leading Whitespace (`parser.trimLeading`)\nStrips leading spaces and tabs from each extracted text unit.\n\n#### Trim Trailing Whitespace (`parser.trimTrailing`)\nStrips trailing spaces and tabs from each extracted text unit.\n\n#### Unescape Source (`parser.unescapeSource`)\nConverts escape sequences (`\\t`, `\\n`, `\\\\`, `\\uXXXX`) into their corresponding characters before extraction.\n\n#### Preserve Whitespace (`parser.preserveWS`)\nPreserves original whitespace in extracted text units.\n\n#### Line Wrapping Mode (`parser.wrapMode`)\nControls how multiple lines combine into a single text unit:\n- **NONE** — Lines separated by `\\n` line feeds\n- **SPACES** — Lines unwrapped and merged with spaces\n- **PLACEHOLDERS** — Inline code placeholders for line breaks\n\n### Inline Codes\n\n#### Enable Inline Codes (`inlineCodes.enabled`)\nActivates regex-based detection of inline codes within extracted text. Matches are converted to inline code placeholders.\n\n#### Code Finder Rules (`inlineCodes.rules`)\nRegex patterns identifying inline codes (format specifiers, escape sequences, etc.). Default rules match C printf format specifiers and escape sequences.\n\n#### Merge Adjacent Codes (`inlineCodes.mergeAdjacent`)\nMerges consecutive inline codes into a single placeholder.\n\n#### Move Boundary Codes (`inlineCodes.moveBoundaryCodes`)\nMoves inline codes at segment boundaries to the skeleton (non-translatable content).\n\n#### Simplifier Rules (`inlineCodes.simplifierRules`)\nRules for simplifying inline code representation.\n\n## Processing Notes\n\n- Auto-detects Unicode BOM to determine encoding; falls back to configured default encoding if no BOM found.\n- UTF-8 output includes BOM only if input was UTF-8 and had a BOM.\n- Output line-break type preserves the original input convention (CR, LF, or CRLF).\n- The regex sub-filter detects a wider range of linebreaks at the cost of lower speed and higher memory usage.\n\n## Limitations\n\nNone known.\n\n## Examples\n\n### Line-by-line with trailing trim\n```json\n{\n  \"parser\": {\n    \"trimLeading\": false,\n    \"trimTrailing\": true,\n    \"unescapeSource\": true,\n    \"wrapMode\": \"NONE\"\n  }\n}\n```\n\n### Paragraph extraction\n```json\n{\n  \"extraction\": { \"extractParagraphs\": true },\n  \"parser\": { \"preserveWS\": true, \"wrapMode\": \"NONE\" }\n}\n```\n\nInput:\n```text\nThis is the first sentence.\nAnd this continues the paragraph.\n\nThis is a new paragraph.\n```\n\nOutput: two text units — the first paragraph as one unit, the second as another.\n\n### Regex extraction (line-by-line)\n```json\n{\n  \"extraction\": {\n    \"rule\": \"(^(?=.+))(.*?)$\",\n    \"sourceGroup\": 2,\n    \"regexOptions\": 8\n  }\n}\n```\n\n### Spliced lines (backslash)\n```json\n{\n  \"extraction\": {\n    \"splicer\": \"\\\\\",\n    \"createPlaceholders\": true\n  }\n}\n```\n",
  "propertySuggestions": {},
  "filterId": "okf_plaintext",
  "wikiUrl": "https://okapiframework.org/wiki/index.php/Plain_Text_Filter"
}
