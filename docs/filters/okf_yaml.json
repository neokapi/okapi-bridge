{
  "filterName": "YAML Filter",
  "overview": "The YAML Filter extracts translatable content from [YAML files](http://www.yaml.org/) and supports [Ruby on Rails](http://rubyonrails.org/) message variables. It extracts string values from key-value pairs, maps, and arrays, assigning each entry a hierarchical name built from the sequence of all parent identifiers (e.g., `fr/activerecord/errors/messages/exclusion`). The filter handles Unicode BOM detection for encoding and preserves original line-break types in output.",
  "limitations": [
    "Wrapped lines in the original source document will be **unwrapped** in the target document — all text will appear on a single line.",
    "If an illegal character is introduced during translation, the user must manually add double or single quotes to keep the target YAML valid. The filter issues a **WARNING** when it can detect this scenario."
  ],
  "processingNotes": [
    "**Encoding detection**: If the file has a Unicode BOM (UTF-8, UTF-16, etc.), that encoding is used. Otherwise, the default encoding specified when opening the document applies.",
    "**Line-breaks**: Output line-break type matches the original input document.",
    "**Identifiers**: Each extracted entry's name is the `/`-separated path of all parent keys, e.g. `fr/activerecord/errors/messages/exclusion`."
  ],
  "parameters": {
    "extraction.extractAll": {
      "help": "Controls the default extraction behavior for key-value pairs.\n\n- **Enabled**: All key/string pairs are extracted, *except* those whose key matches the `excludeKeys` regex.\n- **Disabled**: No key/string pairs are extracted, *except* those whose key matches the `excludeKeys` regex (inverted behavior).\n\nThe `excludeKeys` pattern always acts as the **inverse** of whichever mode is selected here."
    },
    "extraction.includeIsolatedStrings": {
      "help": "Extracts string values that are not directly associated with a key, such as bare strings in arrays:\n\n```yaml\nlist: [one, two, three]\n```\n\nWithout this option, only key-value pairs like `key: value` are extracted."
    },
    "extraction.excludeKeys": {
      "help": "A regular expression matched against keys. Its behavior **depends on the Extract All setting**:\n\n- When **Extract All** is enabled: keys matching this pattern are **excluded** from extraction.\n- When **Extract All** is disabled: keys matching this pattern are **included** for extraction.\n\nWhen **Use Full Path** is also enabled, the regex is matched against the full key path (e.g., `menu/value/popup/menuitem/value`) rather than just the immediate key.",
      "values": "Java regular expression pattern matched against YAML keys (or full key paths when `keys.useFullPath` is enabled).",
      "examples": [
        "`^(id|type|format)$` — exclude specific key names",
        "`^config/.*` — exclude all keys under the `config` parent"
      ],
      "dependsOn": [
        {
          "property": "extraction.extractAll",
          "condition": "Inverts behavior: when extractAll is true, matching keys are excluded; when false, matching keys are included"
        },
        {
          "property": "keys.useFullPath",
          "condition": "When enabled, regex is matched against the full `/`-separated key path instead of just the immediate key"
        }
      ]
    },
    "keys.useAsName": {
      "help": "Uses the YAML key as the `resname` (resource name) of the extracted text unit. This provides a stable, human-readable identifier for each translatable entry — equivalent to `resname` in XLIFF."
    },
    "keys.useFullPath": {
      "help": "Uses the complete `/`-separated path of nested keys as the name instead of just the immediate key.\n\nFor example, given nested YAML:\n```yaml\nmenu:\n  value:\n    popup:\n      label: \"Click me\"\n```\nThe name becomes `menu/value/popup/label` instead of just `label`.\n\nWhen enabled, the `excludeKeys` regex also matches against the full path.",
      "dependsOn": [
        {
          "property": "keys.useAsName",
          "condition": "Must be enabled for this option to take effect"
        }
      ]
    },
    "output.escapeNonAscii": {
      "help": "Escapes characters outside the ASCII range in the output using YAML unicode escape sequences. Useful when the target system or workflow requires ASCII-safe files."
    },
    "output.wrap": {
      "help": "Controls whether long lines are wrapped in the output. Note that regardless of this setting, wrapped lines from the **original** source are always unwrapped during processing — this is a known limitation of the filter.",
      "notes": [
        "Wrapped lines in the original source will always be unwrapped in the output, regardless of this setting."
      ]
    },
    "output.subFilterProcessLiteralAsBlock": {
      "help": "When a sub-filter is configured (e.g., `okf_html`), this option causes YAML literal block scalars (`|`) to also be processed through the sub-filter. Without this, only regular string values are sub-filtered."
    },
    "inlineCodes": {
      "help": "Defines regex patterns to detect inline codes within extracted text. Any match is converted to an inline code placeholder in the extracted content.\n\n**Cannot be used together with sub-filtering** — if a sub-filter is specified, inline code patterns are ignored.\n\nThe default pattern matches:\n- `printf`-style format specifiers (e.g., `%d`, `%s`, `%2$f`)\n- Escape sequences (`\\n`, `\\r`, `\\t`, etc.)\n- Numbered placeholders (`{0}`, `{1}`, etc.)",
      "notes": [
        "**Warning:** This option cannot be used together with the sub-filtering option."
      ]
    }
  },
  "examples": [
    {
      "title": "Ruby on Rails i18n file",
      "description": "A typical Rails locale file showing extractable content. The filter extracts all highlighted string values and assigns hierarchical names based on the key path.",
      "input": "```yaml\nfr:\n  activerecord:\n    errors:\n      template:\n        header:\n          list: [one, two, three]\n          map: {key: value, key2: value2}\n          one: \"Impossible d'enregistrer {{model}}: 1 erreur\"\n          other: \"Impossible d'enregistrer {{model}}: {{count}} erreurs.\"\n        body: \"Veuillez vérifier les champs suivants :\"\n      messages:\n        inclusion: \"n'est pas inclus(e) dans la liste\"\n        exclusion: \"n'est pas disponible\"\n```"
    },
    {
      "title": "Selective extraction with key exclusion",
      "description": "Extract all pairs except metadata keys by using `extractAll: true` with an `excludeKeys` regex pattern.",
      "config": "```yaml\nextraction:\n  extractAll: true\n  excludeKeys: \"^(id|type|version|format)$\"\n```"
    }
  ],
  "filterId": "okf_yaml",
  "wikiUrl": "https://okapiframework.org/wiki/index.php/YAML_Filter"
}
