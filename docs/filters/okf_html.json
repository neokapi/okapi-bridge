{
  "filterName": "HTML/XHTML Filter",
  "overview": "The HTML Filter processes HTML and XHTML documents for localization. It supports both well-formed XHTML and non-conforming HTML via two built-in configurations: `okf_html` (default, lenient parsing) and `okf_html-wellFormed` (strict, enables structural groupings like tables and lists). Configuration is written in YAML and maps HTML elements and attributes to extraction rules that control how the filter treats each tag.",
  "parameters": {
    "parser.assumeWellformed": {
      "help": "Uses an XML-based parser for faster processing. All structural tags that map to `GROUP` rules **must** have matching start and end tags — the filter will error on unclosed tags.\n\nThe default (`okf_html`) configuration uses lenient parsing; the `okf_html-wellFormed` configuration enables this.",
      "notes": [
        "Any structural tags mapped to `GROUP` rules must be well-formed (have both start and end tags), otherwise the filter will return an error."
      ]
    },
    "parser.preserveWhitespace": {
      "help": "Retains the original whitespace in extracted text units. Without this, whitespace is normalized.\n\nIndividual elements can also preserve whitespace via the `PRESERVE_WHITESPACE` rule type (e.g., `<pre>` tags) — this is a global override."
    },
    "elements": {
      "help": "Maps HTML element names to extraction rule configurations. Each key is a **lowercase** element name, and the value defines how the filter processes that element.\n\nEach element rule can include:\n- **`ruleTypes`** — how the filter treats the tag (see below)\n- **`translatableAttributes`** — attributes to extract for translation\n- **`idAttributes`** — attributes providing the segment ID\n- **`conditions`** — restrict the rule to elements matching attribute conditions\n- **`elementType`** — corresponding XLIFF 1.2 `type` value\n\n**Rule types:**\n- `INLINE` — tag inside a text run (`<b>`, `<i>`, `<u>`)\n- `GROUP` — structural grouping (`<table>`, `<div>`) — **must be well-formed** if parser is in well-formed mode\n- `TEXTUNIT` — starts a text unit (`<p>`, `<title>`, `<h1>`)\n- `EXCLUDE` — suppresses extraction until end tag\n- `INCLUDE` — overrides exclusion for child elements\n- `PRESERVE_WHITESPACE` — preserves whitespace as-is (`<pre>`)\n- `ATTRIBUTES_ONLY` — only attributes are localizable, not content\n\n**Conditions** use the syntax `[attribute, operation, value]` where operation is `EQUALS`, `NOT_EQUALS`, or `MATCHES` (regex).",
      "notes": [
        "All element names must be **lowercase** in the configuration, regardless of casing in the document.",
        "Elements with a prefix must be declared with the prefix in single quotes (e.g., `'xml:lang'`)."
      ],
      "examples": [
        "Simple element: `title: { ruleTypes: [TEXTUNIT] }`",
        "Exclude element: `script: { ruleTypes: [EXCLUDE] }`",
        "With translatable attributes:\n```yaml\narea:\n  ruleTypes: [ATTRIBUTES_ONLY]\n  translatableAttributes: [accesskey, area, alt]\n```",
        "Conditional translatable attributes:\n```yaml\ninput:\n  ruleTypes: [INLINE]\n  translatableAttributes:\n    alt: [type, NOT_EQUALS, [file, hidden, image, password]]\n    value: [type, NOT_EQUALS, [file, hidden, image, password]]\n```"
      ],
      "seeAlso": "HTML_Filter#Configuring_Element_Rules"
    },
    "attributes": {
      "help": "Maps HTML attribute names to global extraction rules. These apply across all elements unless overridden by element-specific `translatableAttributes`.\n\nAttribute rule types:\n- `ATTRIBUTE_TRANS` — translatable attribute\n- `ATTRIBUTE_WRITABLE` — writable/modifiable but not translatable\n- `ATTRIBUTE_READONLY` — extracted but cannot be modified",
      "notes": [
        "All attribute names must be **lowercase** in the configuration, regardless of casing in the document.",
        "Attributes with a prefix must be declared with the prefix in single quotes (e.g., `'xml:lang'`)."
      ]
    },
    "inlineCodes": {
      "help": "Define regular expressions to capture spans of extracted text that should be treated as inline codes. Useful for protecting variables or placeholders from modification.\n\nThe rules use the Okapi code finder `#v1` format. Backslashes must be escaped in standard YAML notation, or use YAML block scalar (`|-`) syntax to avoid double-escaping.",
      "examples": [
        "Protect `VAR1`, `VAR2`, etc.:\n```yaml\nuseCodeFinder: true\ncodeFinderRules: |-\n  #v1\n  count.i=1\n  rule0=\\bVAR\\d\\b\n```",
        "Input `<p>Number of files = VAR1</p>` → `VAR1` becomes an inline code"
      ],
      "seeAlso": "HTML_Filter#Inline_Code_Finder"
    },
    "skipEncodingDeclaration": {
      "help": "Controls whether the filter adds an encoding declaration `<meta>` tag to the output when the input has none.\n\nBy default, if the input has no declared encoding, the filter adds a `<meta>` tag (or `<meta />` for XHTML) — but only if a `<head>` element exists in the file."
    }
  },
  "processingNotes": [
    "If the document has an encoding declaration, it is used; otherwise the default encoding specified in filter options is used.",
    "For UTF-8 output: a BOM is written only if the input was also UTF-8 **and** had a BOM. Transcoding from non-UTF-8 to UTF-8 never adds a BOM.",
    "Output line-break type matches the original input.",
    "Character and numeric entities are converted to Unicode. DTD/schema-defined entities pass through unchanged.",
    "By default the filter does **not** create structural groupings (tables, lists, etc.). Use `okf_html-wellFormed` for structural groups.",
    "The filter attempts to clean up common HTML syntax errors (e.g., unquoted attributes) by default during post-processing."
  ],
  "limitations": [
    "Content of `<style>` and `<script>` elements is not extracted.",
    "Server-side script tags (PHP, ASPX, JSP, etc.) are not formally supported and are treated as non-translatable."
  ],
  "examples": [
    {
      "title": "Excluding all content by default",
      "description": "Use `exclude_by_default` to suppress all extraction, then selectively include only specific elements. This produces a more concise configuration when you only need a few elements translated.",
      "config": "```yaml\nexclude_by_default: true\nelements:\n  title:\n    ruleTypes: [TEXTUNIT]\n```"
    },
    {
      "title": "Inline CDATA handling",
      "description": "When CDATA sections appear inline within text and break the flow, enable `inlineCdata` to treat `<![CDATA[` and `]]>` as inline tags.",
      "config": "```yaml\ninlineCdata: true\n```",
      "input": "```html\n<p>Text with <![CDATA[inline]]> CDATA</p>\n```",
      "output": "Extracted as a single text unit with CDATA markers as inline codes."
    },
    {
      "title": "Character entity references in output",
      "description": "Use `escapeCharacters` to force specific extended characters to be output as HTML character entity references instead of raw Unicode.",
      "config": "```yaml\nescapeCharacters: \"© €µÆ\"\n```",
      "input": "```html\n<p>© €µÆĄ</p>\n```",
      "output": "```html\n<p>&copy;&nbsp;&euro;&micro;&AElig;Ą</p>\n```"
    },
    {
      "title": "Quote mode configuration",
      "description": "Control how quote and apostrophe characters are escaped in output. Mode 3 (double quotes only) is commonly used to avoid unnecessary escaping of apostrophes in natural language text.",
      "config": "```yaml\nquoteModeDefined: true\nquoteMode: 3\n```"
    }
  ],
  "filterId": "okf_html",
  "wikiUrl": "https://okapiframework.org/wiki/index.php/HTML_Filter"
}
